<h5 class="card-title">Respostas</h5>

{{#answers}}
        <div class="alert alert-info">
            <h5 class="alert-heading">{{{label}}}</h5>
            <hr>
            <p class="mb-0">{{{answer}}}</p>
        </div>
{{/answers}}

{{#attachment_nonidentified}}
    <div class="alert alert-primary" role="alert">
        <h4 class="alert-heading">Anexo não identificado</h4>
        <hr>
        <div class="attachment">
            <a href="{{fileurl}}" class="btn btn-outline-primary"><i class="fa fa-file-pdf-o"></i> Fazer download do arquivo</a>
        </div>
    </div>
{{/attachment_nonidentified}}